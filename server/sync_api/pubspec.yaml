name: posia_sync_api
description: Hub de sincronizacion multi-tenant POSIA (event log append-only).
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.11.0

dependencies:
  postgres: ^3.5.6
  shelf: ^1.4.2
  shelf_router: ^1.1.4

dev_dependencies:
  http: ^1.6.0
  lints: ^6.0.0
  test: ^1.26.0
